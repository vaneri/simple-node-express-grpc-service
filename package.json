{
  "name": "simple-node-express-grpc-service",
  "workspaces": [
    "express-grpc-client",
    "grpc-models",
    "grpc-server-layer"
  ],
  "scripts": {
    "clean": "tsc --build --clean",
    "build": "tsc --build",
    "watch": "tsc --build --watch",
    "start:server": "node grpc-server-layer/dist/server",
    "start:client": "node express-grpc-client/dist/main"
  },
  "devDependencies": {
    "typescript": "^5.1.3"
  }
}
